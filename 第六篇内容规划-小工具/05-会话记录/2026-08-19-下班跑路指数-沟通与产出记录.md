# 下班跑路指数 · 沟通与产出记录

日期：2026-08-19

## 最终判断

本轮小工具名称最终统一为「下班跑路指数」。当前交付重点不是继续加内容，而是保证小红书预览环境里标题、题干、按钮和结果卡都能稳定展示。

## 本轮主要需求

- 移除头像和 ID，避免结果卡出现个人身份信息。
- 将小工具命名收束为「下班跑路指数」。
- 首页压缩到一屏，降低进入测试前的阅读成本。
- 答题页题干必须完整可见，选项选中态要更明显，并保留「上一题」。
- 答题页顶部中间区域展示小工具名称，避开小红书返回/分享浮层。
- 结果页压缩到一屏，保存按钮下方并列放置「我不服」和「回到首页」。
- 增加回到首页功能，用户可查看最近一次测试结果。
- 图标改成 PNG，方向为安全出口式小人跑路。
- 小红书相册保存逻辑保留，减少截图提示文案。

## 已完成产出

- 当前源码：`02-小工具源码/下班跑路指数/`
- 最终上传包：`03-交付包/下班跑路指数.zip`
- 关键截图：`04-预览截图/`
- 开发规格：`01-规划文档/2026-08-18-post-06-小工具-下班跑路指数-开发规格.md`

## 关键实现记录

- `index.html`
  - 页面标题、首页标题、答题页顶部标题统一为「下班跑路指数」。
  - 结果页按钮改为：主按钮「保存到相册」，次级并列按钮「我不服」「回到首页」。
  - favicon 引用从 SVG 改为 PNG。

- `css/style.css`
  - 答题页顶部新增工具名展示区。
  - 进度标题居中，右侧「点选即下一题」隐藏，避免与小红书浮层冲突。
  - 题干样式改为更稳的黑体可读样式，避免预览中被裁切。
  - 选项整体下移，保留底部「上一题」一屏可见。
  - 结果页操作区改为按钮化设计。

- `js/app.js`
  - 增加回首页能力。
  - 首页支持查看最近一次测试结果。
  - 「我不服」保留上次结果用于对比。

- `js/canvas.js`
  - 结果卡顶部标识统一为「下班跑路指数」。
  - 结果卡尺寸压缩，适配一屏展示。

- `js/config.js`
  - 本地存储 key 改为 `xblp_last_result`。

- `favicon.png`
  - 512 x 512 PNG 图标。
  - 绿色安全出口视觉，小人向出口跑路。

## 校验摘要

- JS 语法检查通过：`app.js`、`canvas.js`、`config.js`、`results.js`。
- 旧名残留扫描通过：当前小工具源码中没有早期中文命名、旧存储 key 和旧英文标识。
- 压缩包检查通过：包内包含 `favicon.png`，不包含 `favicon.svg`。
- 360 x 780 视口下检查通过：
  - 答题页顶部工具名居中；
  - 第 1 题题干完整；
  - 第 8 题避开小红书顶部浮层；
  - 第 12 题长题干场景下「上一题」仍在一屏内；
  - 结果页按钮区仍在一屏内。

## 注意

当前仓库中原始源码目录仍沿用历史开发路径。对外展示名、页面名、结果卡名和交付包名已经统一为「下班跑路指数」。
